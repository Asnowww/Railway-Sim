package com.railwaysim.vehicle.drivercab;

/** Origin seat of a driver-cab PLC input: the browser console or the physical PLC on the machine-room LAN. */
public enum DriverCabControlSource {
    FRONTEND,
    PHYSICAL
}
