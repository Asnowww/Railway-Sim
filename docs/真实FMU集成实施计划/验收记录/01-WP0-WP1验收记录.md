# 分计划01验收记录：WP0～WP1

## 1. 版本与范围

| 项目 | 结果 |
|---|---|
| 分支 | `elec-and-train` |
| WP0提交 | `4657398 feat(fmu): 冻结车辆FMU跨服务接口契约` |
| WP1提交 | `1585ade feat(vehicle): 统一9300与9000车辆参数来源` |
| 参数文件 | `config/train_params.yaml` |
| 参数集ID | `sha256:1e9876fadddf765471ab6e57da39de909b2074ccd9687089bc5f2680f90e6994` |
| FMU状态 | 尚未导出真实FMU；本阶段只完成契约和参数基线 |

## 2. WP0验收证据

| 验收项 | 证据 | 结果 |
|---|---|---|
| 映射分组 | `config/fmu_mapping.yaml`包含`parameters`、`initialState`、`stepInputs`、`metadata`、`outputs` | 通过 |
| JSON Schema | 请求、成功响应、错误响应及三份示例由Draft 2020-12校验 | 通过 |
| 9300 DTO | `FmuContractDtoTests`反序列化请求/响应示例 | 通过 |
| 9000映射 | `test_contract.py`验证生命周期、再生能力和Boolean字段 | 通过 |
| 9200兼容字段 | 自测确认约束响应包含数值型`regenPowerAvailableWatts`；WP6前固定为0 | 通过 |
| 8080向后兼容 | `FmuVehiclePhysicsAdapterTests`使用包含新增字段的9000响应 | 通过 |
| 功率口径 | `tractionPowerWatts/regenPowerWatts`为电网侧，机械功率使用独立字段 | 通过 |

## 3. WP1验收证据

| 验收项 | 证据 | 结果 |
|---|---|---|
| 9300真实读取YAML | Spring Boot启动后`GET /vehicle-runtime/parameters`返回参数源、质量和3.2 MW机械功率 | 通过 |
| 9000真实读取YAML | `GET /fmu/metadata`返回同一参数源和参数集ID | 通过 |
| 跨语言哈希一致 | 9300、9000和`shasum -a 256`均返回`1e9876...e6994` | 通过 |
| 参数校验 | Java/Python均覆盖非法效率、未知字段和矛盾电压阈值 | 通过 |
| 参数不匹配 | 9000对错误`parameterSetId`返回HTTP 409和`FMU_PARAMETER_SET_MISMATCH` | 通过 |
| 9300去硬编码 | 源码检索未发现质量、力、功率、效率、Davis和保护电压的重复车辆常量 | 通过 |
| 总质量每步输入 | `VehicleControlQueue`每步由YAML空车/载荷参数计算`trainMassKg` | 通过 |
| fallback功率口径 | 测试确认机械功率不超过3.2 MW且`P_mech=P_grid×0.88` | 通过 |

## 4. 执行命令与结果

```bash
mvn -f vehicle-runtime-service/pom.xml test
# 13 tests, 0 failures

PYTHONPATH=fmu-service fmu-service/.venv/bin/python \
  -m unittest discover -s fmu-service/tests -p 'test_*.py'
# 7 tests, 0 failures

PYTHONPATH=power-network-service power-network-service/.venv/bin/python \
  -m app.self_test
# passed

mvn -f backend/pom.xml \
  -Dtest=FmuVehiclePhysicsAdapterTests,VehicleRuntimeIntegrationServiceTests test
# 8 tests, 0 failures
```

实际启动验证使用临时端口19300和19000；两端返回相同`parameterSetId`，错误哈希请求返回HTTP 409。验证完成后两个临时服务均已停止。

## 5. 当时边界及后续状态

- 本记录验收时9000仍是`BaseHTTPRequestHandler + SimpleFallbackModel`；该边界现已由WP4关闭，当前生产入口为FastAPI + FMPy真实FMU执行器。
- 9200只完成`regenPowerAvailableWatts`接口冻结，真实预算仍由WP6实施。
- 本记录验收时Modelica仍是草案；该边界现已由WP2关闭，当前模型已按冻结映射实现并通过真实FMU测试。
- 本阶段没有修改`App.vue`。

WP2～WP4后续证据见`docs/真实FMU集成实施计划/验收记录/02-WP2-WP4验收记录.md`。
