# 供电与车辆模块下一阶段开发计划

> 原计划编制日期：2026-07-10  
> 审查日期：2026-07-11  
> 审查基线：当前 `elec-and-train`（`aac06d2`）  
> 阶段边界：中期答辩后、FMU 物理模型部署前  
> 状态口径：仅以当前仓库中的代码、配置、测试和构建产物为准

## 1. 文档目的

本文档在原计划基础上完成三项收口：

1. 已完成任务仅保留结果和证据，不再重复实施步骤。
2. 单列当前实现与原计划的冲突，避免继续按过时前提开发。
3. 保留尚未实现的工作、验收要求和暂缓范围，作为下一阶段任务基线。

下一阶段目标调整为：在不重复建设现有 FMU、车辆运行时和供电闭环的前提下，补齐“司机台/自动策略—安全决策—车辆物理—停车评价—持久化—前端回放”的可验收链路。

## 2. 审查结论

### 2.1 已完成任务（精简）

| 能力 | 当前结果 | 主要证据 |
|---|---|---|
| 五分区供电拓扑 | 已配置 P01～P05，并包含第三轨区段、变电所、整流、断路器、隔离开关、回流和杂散电流监测对象 | `config/power_third_rail.yaml` |
| 供电—车辆权威 SPLIT 闭环 | 9300 按分区聚合并向 9200 写入负荷；9200 返回新约束；8080 外部供电路径只 bootstrap/查询镜像，不写车辆负荷 | `PowerNetworkLoadClient`、`VehicleRuntimeManager`、`PowerIntegrationService`、`application-split.yml` |
| 车辆动力学状态机 | 中央已具备自检、牵引、巡航、惰行、站停、离站、MA/超速/安全制动、失电/降额和过载降额状态 | 两处 `TrainDynamicsState`、`OnboardTrainSubsystem`、`VehicleControlQueue` |
| 真实 FMU 基线 | 已有 Modelica 模型、FMI 2.0 Co-Simulation 构建流程、FMPy 多实例运行时、FMU 清单和可运行构建产物 | `fmu-service/modelica/TrainTractionBrake.mo`、`fmu-service/build/TrainTractionBrake.fmu`、`FmuHttpVehiclePhysicsExecutor` |
| FMU 参数曲线升级 | 已完成参数 Schema v2、车辆曲线、跨 8080/9300/9000 参数映射及闭环验证 | 提交 `7dfeb3c` 及 `docs/真实FMU集成实施计划/` |
| 外部车辆运行时 | 9300 已维护车辆实例、控制队列、仿真队列、FMU 批处理、Java fallback、中央登记与供电约束交互 | `vehicle-runtime-service` |
| 司机台协议基础 | 已具备 46 字节输入、26 字节输出编解码、REST 入口、TCP 适配和状态回传 | `DriverCabPlcCodec`、`DriverCabController`、`DriverCabAdapter` |
| 动力学和供电基础持久化 | 已记录车辆物理快照、能耗与供电分区状态 | `SimulationPersistenceService`、`database/schema.sql` |
| 真实线路约束 | 车辆控制已可使用 XLS 线路数据中的限速、坡度、车站等约束 | `SpreadsheetLineDataLoader`、`OperationalLineData` |

以上能力不再作为下一阶段“待开发项”；后续只做缺陷修复、契约统一和验收补证。

### 2.2 当前实现与原计划冲突

| 编号 | 原计划表述/假设 | 当前实现 | 处理结论 |
|---|---|---|---|
| C-01 | 计划的主状态包含 `DEPARTING_STATION` | 中央状态机已有该状态，9300 状态机缺失 | **[已修复]** 于 2026-07-11 提交 `ea0d736`，补齐 9300 `TrainDynamicsState.DEPARTING_STATION` 枚举值及 40m 离站保护窗口，`mvn test` 21 项通过 |

## 3. 下一阶段必须完成

### WP-01 需求基线与追踪

- [ ] 将司机台操作者、车辆控制维护人员、供电调度/维护人员、系统管理员和演示人员纳入正式需求角色。
- [ ] 将 `VHC-FR`、`PWR-FR`、`PV-FR` 及对应 NFR 纳入软件需求说明书和需求变更记录。
- [ ] 建立需求追踪矩阵，至少关联需求、设计、实现文件、API、测试和状态。
- [ ] 在需求、架构、API 和部署文档中统一 8080/9300/9200 的数据所有权。
- [ ] 明确标记真实 FMU、Java fallback、Mock、Stub、LIVE、STALE 和 DEGRADED，答辩材料不得混用。

必须明确的业务规则：

- MANUAL、AUTO、INTELLIGENT、DEGRADED/EMERGENCY 的进入、退出和恢复条件。
- 紧急制动 > ATP/MA > 车门/自检/制动可用性 > 供电约束 > 人工命令 > 自动/智能命令 > 常规巡航。
- 手动模式不能绕过 MA、车门、自检和供电约束。
- 目标停车点的唯一来源优先级、停车误差符号、越站阈值、零速阈值和稳定时间。
- 9200/9300 断线时的超时、旧值有效期、fallback、安全停车和恢复条件。
- reset 新建 runId、重置当前状态但保留历史记录。

验收：每条需求具有优先级、输入、处理规则、异常规则、验收方法，并至少关联一个测试用例。

### WP-02 司机台控制与统一安全决策

- [ ] 建立明确的 `VehicleOperationMode`、`DriverControlCommand`、`AutomaticControlCommand`、`VehicleSafetyConstraint`、`VehicleControlDecision` 和优先级解析器。
- [ ] 将方向、主手柄牵引、常用制动、紧急制动、车门和 ATO 请求真正接入下一 tick 的 `VehiclePhysicsInput`。
- [ ] 模式切换必须由服务校验，不得通过修改展示字符串直接改变物理行为。
- [ ] 紧急制动强制 `traction=0`、`brake=1`、`emergencyBrake=true`。
- [ ] 异常帧、越界字段、过期数据和未知列车返回明确 HTTP 状态及结构化错误码。
- [ ] `POST /api/vehicle/driver-cabs/{trainId}/plc-input` 返回 accepted、operationMode、decisionSource、reasonCode 和 updatedAt。
- [ ] 提供最终控制决策查询和控制决策轨迹。

验收测试：牵引、常用制动、零位、紧急制动覆盖、开门禁牵引、钥匙关闭、MA 耗尽、供电失电、输入过期、模式切换、LOCAL/EXTERNAL_HTTP 语义一致；输入至状态更新 P95 < 200 ms。

### WP-03 停车控制与评价

- [x] ~~统一中央与 9300 的动力学状态枚举，补齐 9300 的 `DEPARTING_STATION` 和离站保护行为。~~（已于 `ea0d736` 完成）
- [ ] 将目标停车点、有效制动减速度、坡度/载重/制动单元补偿、安全缓冲、分段制动阈值和稳定窗口配置化。
- [ ] 规则型自动停车必须输出可解释决策，并在 MA 缩短时切换到安全授权终点。
- [ ] 建立 `TrainStopResult`：runId、场景、模式、算法版本、目标/实际位置、误差、越站、时长、最大速度/减速度、制动切换次数、紧急制动次数和成功状态。
- [ ] 停车误差定义为 `actualPosition - targetPosition`；记录绝对误差、越站、平稳性、安全性、重复成功率和方差。
- [ ] 明确再生制动不可用时空气制动的替代关系。

验收测试：完整手动停车；自动停车连续 10 次；提前停车/精确停车/越站；载重、制动单元、坡度影响；降额/失电；MA 缩短；FMU 失败后状态连续；无 NaN/Infinity/负速度；正常 Mock 场景平均绝对误差 < 1 m，未达标时输出统计和原因。

### WP-04 供电闭环补证与恢复

五分区拓扑、SPLIT 权威闭环和真实负荷写入已完成，本工作包只保留未验收项：

- [ ] 验证五分区无重叠、无空隙，边界只匹配一个分区。
- [ ] 负里程、拓扑空隙和超范围位置返回 `UNKNOWN` 或安全约束，不得默认最后分区。
- [ ] 用自动化测试证明 SPLIT 下 8080 不写负荷、9300 每 tick 只写一次、9200 是唯一供电约束计算方。
- [ ] 完整记录牵引功率/电流、再生功率、已吸收/未吸收再生功率、净功率/电流和受影响列车。
- [ ] 统一 UP → DEGRADED → STALE → FALLBACK → RECOVERING → UP 的服务恢复语义。
- [ ] 恢复前重新 bootstrap、校验版本和 tick 连续性，防止双写、负荷突跳和旧 tick 覆盖。
- [ ] 补齐欠压、失电、跳闸、过流、检修闭锁、再生吸收和服务中断/恢复测试。

### WP-05 故障、告警与操作审计

- [ ] 建立统一结构化故障、事件、影响分析和告警模型，覆盖司机台通信/紧急制动、车门、制动/牵引不可用、过载、FMU/运行时失败、欠压/失电/过流/跳闸/检修闭锁、供电通信失败和非法控制数据。
- [ ] 危险故障必须导向切除牵引、制动、fallback 或安全等待。
- [ ] 告警直接消费结构化故障、健康、决策和数据质量事件，不依赖中文原因文本推断。
- [ ] 提供故障注入/清除、告警查询/确认和影响范围接口；危险写操作必须二次确认。
- [ ] 日志记录 operator、operationType、targetRef、before/afterState、reason、traceId、runId、tick 和时间戳。
- [ ] 修复并验证 H2/MySQL 操作日志真实写入，不得吞掉 SQL 错误后让测试通过。

验收：紧急制动一个控制周期内生效；供电故障 5 秒内可观察状态、车辆响应和告警；恢复前重新校验；所有人工操作可按 traceId/runId 查询。

### WP-06 统一运行记录与持久化

- [ ] 将调度域现有 `simulationRunId` 提升为全仿真运行标识，并传入 tick、车辆、供电、控制和持久化链路；不得另建一套互不关联的 runId。
- [ ] 保留现有调度命令、车站事件和扰动记录中的 runId 实现；建立缺失的 `simulation_run`、`vehicle_control_command_log`、`train_stop_result` 和 `power_vehicle_fault_record`。
- [ ] 为 `train_physics_snapshot`、`train_energy_record`、`power_section_record` 增加并写入统一 `simulation_run_id`。
- [ ] 场景启动生成 runId；完成、失败或 reset 时更新状态和摘要。
- [ ] reset 结束旧 run、创建新 run、清理临时状态但不删除历史；未完成场景标记 `CANCELLED_BY_RESET`。
- [ ] 建立车辆命令、物理、供电、停车、故障的 JDBC 仓储；内存实现只用于单测/离线模式，避免 Bean 重复注册。
- [ ] 提供运行列表/详情及车辆、命令、供电、停车、故障历史查询接口。
- [ ] 后端重启后历史仍可查询，所有曲线按 runId/tick/trainId/traceId 对齐。

### WP-07 正式前端与回放

- [ ] 建立正式供电车辆页面，接入真实 REST/WebSocket 数据。
- [ ] 展示 runId、场景、车辆/供电模式、物理模型、控制决策、动力学、能耗、五分区状态、健康、数据质量、停车评价、故障、影响范围和告警。
- [ ] 明确区分 LIVE、STALE、FALLBACK、MOCK、STUB；静态数据必须替换或明确标记 Mock。
- [ ] 故障注入、模式切换和供电操作二次确认；危险操作仅在演示/调试模式开放。
- [ ] 页面卸载关闭订阅，断线重连不得重复提交操作。
- [ ] 支持历史 runId 回放；回放只读，不重新执行控制算法。
- [ ] 动态加载 ECharts 和大型页面，并补齐前端自动化测试。

### WP-08 三服务联调与发布验收

- [ ] 同时启动并校验 8080 split、9300 split、9000 FMU 和 9200 供电配置。
- [ ] 完成手动停车、规则型自动停车、欠压、失电、司机台紧急制动、9300 中断、9200 中断和恢复场景。
- [ ] 五列车在五分区连续运行 10 分钟，无崩溃、无负荷双写、无非法数值。
- [ ] 验证控制延迟 P95 < 200 ms、供电/车辆状态刷新周期 <= 500 ms。
- [ ] 形成可重复执行的集成测试、测试报告、联调报告、演示脚本和运行手册。

## 4. 保留的测试清单

| 类别 | 必须保留的用例 |
|---|---|
| 司机台与安全 | 主手柄牵引/制动/零位、紧急制动、开门禁牵引、MA 覆盖、供电覆盖、通信过期、模式切换 |
| 停车 | 手动停车、自动停车 10 次、越站、载重、制动单元、坡度、MA 缩短、误差与平稳性统计 |
| FMU/运行时 | FMU 批处理、实例隔离、失败 fallback、状态连续、恢复重同步、模型/数据质量标识 |
| 供电 | 五分区映射、非法位置、欠压、失电、过流、检修闭锁、再生吸收、唯一负荷写入 |
| 服务故障 | 9200/9300 中断、STALE/FALLBACK、本地接管或安全停车、恢复无双写和旧 tick 覆盖 |
| 数据库 | runId 贯通、命令/停车/故障记录、操作日志 H2/MySQL 写入、reset、重启后历史查询 |
| 前端 | 真实状态、错误码、数据质量、危险操作确认、断线重连、历史回放 |
| 非功能 | 五列车 10 分钟、控制延迟、刷新周期、数据增长和无非法物理值 |

## 5. 交付文档

以下文档尚未按原计划建立或收口，继续保留：

- 软件需求说明书、需求变更记录、供电车辆需求追踪矩阵。
- 供电车辆概要设计、车辆停车控制设计、车辆运行时设计。
- 供电权威闭环 ADR、API 契约、数据库设计、集成契约和状态机说明。
- FMU/RT-LAB/Java fallback 模型决策与部署说明。
- 测试计划、测试用例、测试报告和三服务联调报告。
- 演示脚本、部署运行手册和已知问题清单。

现有 `docs/真实FMU集成实施计划/` 及验收记录作为真实 FMU 和三服务编排的既有证据，不重复新建同内容文档；新文档必须引用并补足司机台、停车评价、runId、故障审计和前端回放部分。

## 6. 依赖顺序与优先级

### P0：安全和闭环阻断项

1. 司机台命令进入 `VehiclePhysicsInput`。
2. 统一控制优先级，保证紧急制动、MA、车门、自检和供电约束不可被覆盖。
3. ~~统一中央/9300 的状态枚举，补齐离站状态。~~（已于 `ea0d736` 完成，从 P0 划除）
4. 用测试证明 SPLIT 唯一负荷写入、非法供电位置安全处理和服务过期安全响应。
5. 修复并验证操作日志持久化。

### P1：核心验收能力

1. 手动/自动停车与停车评价。
2. runId 全链路持久化、reset 和历史查询。
3. 统一故障、告警、影响分析和审计。
4. 正式供电车辆页面和只读回放。
5. 三服务故障/恢复与 10 分钟连续运行验收。

### P2：增强能力

1. INTELLIGENT 模式、智能停车或参数自适应，并与规则基线对比。
2. 更完整的前端自动化、性能和长期稳定性测试。

## 7. 本轮暂缓（原计划保留）

- 工业级 PSCADA 全点表、真实保护继电器和真实牵引变电所控制协议。
- 完整直流牵引网潮流、短路电流、谐波和保护配合计算。
- 超过五个供电分区的全线路精细供电网络。
- 超过 20 列车的高并发压力测试。
- 工业级 TCMS、ATO、ATP 和真实安全认证逻辑。
- 复杂机器学习、深度强化学习停车控制和 PHM/预测性故障诊断。
- 多套 FMU、RT-LAB 和本地模型运行中无缝热切换。
- 真实司机台、真实 RT-LAB 和真实供电设备全部同时在线的最终硬件联调。

## 8. 完成定义

仅当以下条件全部满足，供电与车辆下一阶段工作才可标记完成：

- 需求、设计、API、代码和测试可双向追踪，8080/9300/9200 权责一致。
- 司机台牵引、常用制动、紧急制动、方向、车门和模式命令真实进入车辆控制闭环。
- 所有安全约束均优先于人工、自动和智能牵引命令。
- 手动停车和规则型自动停车可重复演示，并生成完整停车结果；自动停车 10 次有可解释统计。
- 五分区边界与非法位置测试通过；SPLIT 下 9300 唯一写负荷、9200 唯一算供电约束、8080 只编排和读取。
- 欠压、失电、过流、检修闭锁、司机台紧急制动及服务中断均产生可观察的安全响应和告警。
- 控制、物理、能耗、供电、故障和停车结果使用同一 runId，可在重启后查询和只读回放。
- 前端使用真实数据并正确区分 LIVE、STALE、FALLBACK、MOCK 和 STUB。
- 后端、车辆运行时、FMU、供电服务、数据库、前端、三服务联调、控制延迟和连续运行测试全部通过。
- 测试报告、联调报告、演示脚本、运行手册、已知问题和需求追踪矩阵反映真实状态。
