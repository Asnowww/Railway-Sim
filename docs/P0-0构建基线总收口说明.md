新增统一入口：

```bash
PYTHON_312=python3.12 ./scripts/verify-all.sh --bootstrap
```

该入口依次覆盖：

1. 8080 后端 Maven 测试。
2. 9300 车辆运行时 Maven 测试。
3. 9200 供电服务 unittest。
4. 9000 Linux/amd64 Docker pytest。
5. 前端 `vue-tsc --noEmit` 与生产构建。

`.github/workflows/verify.yml` 将以上五组拆为独立 CI job。任一 job 未运行或失败，CI 不应显示整体通过。
