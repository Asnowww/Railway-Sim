# you asked

message time: 2026-07-07 13:05:16

廖宇轩负责前端与综合监控展示工作，主要完成综合监控首页、线路运行状态可视化、客流热力图、客流统计看板、统一告警列表、调度结果展示以及前后端联调与展示优化；车辆、信号、轨道、供电、调度等业务接口字段由对应模块负责人主写。
刘倩负责运营调度模块，主要完成运行计划配置、发车间隔调整、运行扰动识别、调度策略设计、客流驱动调度联动，以及调度结果展示与策略评估。
黄旭涛负责信号与轨道模块，主要完成轨道拓扑建模、区段占用状态维护、道岔状态管理、信号机显示、进路联锁、移动授权计算，以及故障注入与安全响应演示。
方首岳负责供电与车辆模块，主要完成供电分区状态、电压电流监测、供电故障告警、车辆运行状态、载客量与故障状态模拟。
这是人员的模块分工，把上述交互协议的编写任务分配到人。

---

# chatgpt response

下面根据《上京地铁系统立项说明书》重新审核接口覆盖范围。这里的 **3/3/2/2 是大致工作量比例，不是协议数量**。接口分工按“模块对/接口族”归并：两个模块之间的双向交互统一交给一个人主责，协作人只补充本模块字段、约束和验收意见。

## 1. 覆盖性审核结论

| 审核项 | 立项说明书中的要求 | 覆盖结论 |
|---|---|---|
| 综合监控接入 | 接收车辆、信号、轨道、供电、客流、调度状态，展示线路、设备、客流、告警和运营指标 | 必须覆盖；廖宇轩负责前端展示与字段映射，各业务字段由来源模块负责人主写 |
| 运营调度闭环 | 运行计划、扰动识别、调度参数调整、输出至车辆/信号等子系统、结果反馈与评估 | 必须覆盖；刘倩主责调度相关接口族 |
| 信号与轨道安全链路 | 车辆位置上报、轨道占用、进路请求、联锁检查、MA/限速下发、故障安全响应 | 必须覆盖；黄旭涛主责信号轨道安全接口族 |
| 供电与车辆运行链路 | 牵引供电状态、供电故障、车辆牵引/制动、再生制动、能耗和故障状态 | 必须覆盖；方首岳主责供电车辆接口族 |
| 客流联动 | 客流统计、热力图、短时预测、客流异常告警、客流驱动调度 | 必须覆盖；客流业务归刘倩主责调度联动，廖宇轩负责热力图和看板展示 |
| 统一告警与影响分析 | 汇聚车辆、信号、供电、轨道、客流告警，计算影响区段、列车和旅客范围并推送调度 | 必须覆盖；各模块提供告警语义，廖宇轩统一前端列表和展示口径 |
| 数据通信与心跳 | 各子系统链路心跳、通信状态指示、数据中断告警、恢复后补传 | 原分工容易漏；需作为通用接口规范写入 |
| 仿真同步 | 各模块支持仿真总控时间同步、加速、减速、暂停 | 原分工容易漏；需作为全模块公共字段和公共接口写入 |
| 权限、配置与日志 | 阈值配置、权限控制、操作日志、接口报文记录、敏感配置二次确认 | 原分工容易漏；廖宇轩负责前端配置入口，业务模块提供可配置项和日志字段 |
| 维修、检修与经济能耗扩展 | 供电检修停送电、车辆维修工单、能耗/经济分析预留接口 | 需要按项目范围决定实现深度；接口说明中至少保留扩展字段和边界 |

## 2. 分工原则

| 原则 | 说明 |
|---|---|
| 两模块交互统一主责 | 例如“调度—车辆”同时包含调度参数下发、车辆执行反馈、异常回传和调度评估字段，不再拆成两个方向分给两个人 |
| 不按协议数量机械平均 | 方首岳、刘倩、黄旭涛、廖宇轩工作量大致按 3:3:2:2 控制，但具体接口族数量可不相等 |
| 主责人写闭环 | 主责人统一写触发条件、输入字段、输出字段、异常处理、刷新周期、日志字段和联调验收点 |
| 协作人补业务约束 | 协作人补充本模块字段含义、安全约束、取值范围、异常码和展示需求 |
| 公共要求不遗漏 | 每个接口族都要补充心跳状态、时间戳、仿真时钟、数据质量、错误码、日志追踪 ID 和权限要求 |

## 3. 接口族分工

| 接口族 | 主责人 | 协作人 | 覆盖内容 |
|---|---:|---:|---|
| 调度—车辆接口族 | 刘倩 | 方首岳 | 发车间隔、停站时间、区间运行参数、扣车/折返、车辆执行状态、位置速度、到发时间偏差、扰动恢复效果 |
| 调度—信号/轨道接口族 | 刘倩 | 黄旭涛 | 进路请求、运行调整意图、临时限速、信号安全校验结果、进路建立/拒绝原因、MA/限速执行反馈 |
| 调度—供电接口族 | 刘倩 | 方首岳 | 供电能力下降、受影响分区/区段/列车、可用供电能力、限速/扣车/折返策略、处置结果反馈 |
| 调度—客流接口族 | 刘倩 | 廖宇轩 | 客流阈值、客流预测、站台候车人数、断面客流、客流异常触发调度、调度前后客流影响评估 |
| 调度—综合监控接口族 | 刘倩 | 廖宇轩 | 当前运行模式、调度指令状态、策略执行状态、调度前后对比、策略评估指标、调度事件记录 |
| 供电—车辆接口族 | 方首岳 | 黄旭涛 | 牵引供电状态、电压电流、牵引负荷、再生制动回馈、能耗累计、供电异常对车辆运行限制 |
| 供电—综合监控接口族 | 方首岳 | 廖宇轩 | 外电/牵引变电所状态、供电分区、电压电流、跳闸/失压、电压越限、故障区段、影响范围、供电告警 |
| 车辆—综合监控接口族 | 方首岳 | 廖宇轩 | 车次号、位置、速度、驾驶模式、载客量、满载率、车辆故障码、拥挤预警、车辆故障影响范围 |
| 车辆—信号接口族 | 方首岳 | 黄旭涛 | 车辆速度/载重/门状态/制动牵引可用状态/车载信号接口状态上报，MA、限速、制动命令投影和上线/下线会话语义 |
| 车辆—轨道接口族 | 方首岳 | 黄旭涛 | 车辆位置、车头车尾里程、列车长度与轨道区段、限速、坡度、曲线半径、站距约束之间的映射 |
| 供电/车辆—维修检修与能耗扩展接口族 | 方首岳 | 刘倩、廖宇轩 | 检修停送电状态、维修工单、故障闭环、能耗数据、再生制动数据、经济分析预留字段；若项目范围收缩，可作为预留接口 |
| 信号—轨道接口族 | 黄旭涛 | 刘倩 | 区段占用、道岔定位/反位、道岔锁闭/解锁、进路冲突检查、信号开放条件、故障安全降级 |
| 信号/轨道—综合监控接口族 | 黄旭涛 | 廖宇轩 | 信号机显示、进路状态、MA 数据、联锁状态、轨道占用、道岔方向、红光带/失表/断丝等故障告警 |
| 轨道基础数据与线路查询接口族 | 黄旭涛 | 方首岳、刘倩 | YAML 线路拓扑、区段 ID、限速值、坡度值、路径查询、拓扑切换、车辆位置到区段映射规则 |
| 综合监控前端接入规范 | 廖宇轩 | 全员 | 字段展示名、单位、枚举值、颜色规则、图层开关、空状态、异常状态、刷新频率、大屏/PC 自适应验收 |
| 统一告警、通信与公共支撑接口族 | 廖宇轩 | 全员 | 统一告警字段、影响范围展示、确认/屏蔽、链路心跳、数据中断告警、仿真时钟、权限配置、操作日志、接口报文记录 |

## 4. 方首岳负责部分细化

方首岳负责供电与车辆相关接口族的业务字段、状态语义、异常/告警语义和联调验收口径。这里不落实代码实现，只细化接口设计范围。按照当前架构边界，供电模块输出供电状态和运行约束，不直接操控列车；车辆物理输入统一经单车基层智能子系统边界进入车辆侧，不由调度、信号、供电或轨道模块直接调用车辆物理模型。

### 4.1 方首岳主责接口族

| 接口族 | 主责内容 | 需要写清楚的字段/状态 | 异常与告警 | 主要协作边界 |
|---|---|---|---|---|
| 供电—车辆接口族 | 定义供电状态如何影响车辆取流、牵引、制动和能耗反馈 | 供电分区 ID、列车 ID、列车位置、供电状态、牵引网电压、电流、可用牵引功率、取流状态、牵引功率、再生制动功率、能耗累计 | 失电、欠压/过压、过流、跳闸、接触轨失压、再生制动回馈异常、车辆取流异常 | 与黄旭涛确认车辆位置到区段/供电分区映射；与刘倩确认供电限制是否需要触发调度策略 |
| 供电—综合监控接口族 | 定义供电状态、供电告警和影响范围上报给综合监控的业务字段 | 外电状态、牵引变电所状态、供电分区状态、电压、电流、负荷、带电/失电、跳闸状态、故障区段、受影响列车列表 | 变电所跳闸、接触轨失压、电压越限、馈线过流、供电能力下降、通信中断 | 廖宇轩负责展示颜色、图层和告警列表；方首岳负责字段含义、等级建议和影响范围来源 |
| 车辆—综合监控接口族 | 定义车辆运行状态、载客量、拥挤预警和车辆故障上报字段 | 车次号、列车 ID、位置、速度、加速度、驾驶/运行模式、牵引状态、制动状态、车门状态、载客量、满载率、故障码、故障等级 | 满载率超过阈值、车辆故障、制动异常、牵引异常、受流异常、降级运行、禁止发车 | 廖宇轩负责线路图、车辆图标、满载率色标和故障展示；方首岳负责车辆状态字段和故障语义 |
| 车辆—信号接口族 | 定义车辆向信号提供的安全状态，以及接收信号 MA/限速/制动命令投影所需字段 | 列车 ID、速度、零速状态、门状态、制动可用、牵引可用、受流状态、载重、故障限速、运行模式、车辆故障状态、控制会话状态 | 速度异常、车门未锁闭、制动不可用、牵引不可用、车载信号接口异常、无 MA、MA 耗尽、未并入信号网 | 黄旭涛负责 MA、限速和信号安全语义；方首岳负责车辆侧状态、遥测和能力字段 |
| 车辆—轨道接口族 | 定义车辆位置与轨道约束之间的字段映射 | 列车 ID、车头公里标、车尾公里标、列车长度、当前位置、速度、区段 ID、轨道限速、坡度、曲线半径、站距 | 位置数据异常、区段映射失败、轨道故障导致约束变化、曲线/坡度约束缺失 | 黄旭涛负责轨道区段、占用、道岔和拓扑；方首岳只消费轨道约束并输出车辆位置 |
| 供电/车辆—维修检修与能耗扩展接口族 | 保留检修、维修、能耗和经济分析所需字段边界，避免后续返工 | 检修状态、停送电状态、作业闭锁、维修工单号、故障闭环状态、维修结果、牵引能耗、再生制动回馈、能耗成本预留字段 | 检修闭锁冲突、误送电风险、维修未闭环、能耗数据缺失、经济分析字段缺失 | 若项目范围收缩，本接口族只写预留字段和不实现说明；是否实现业务流程由组长确认 |

### 4.2 方首岳需要交付的接口设计内容

| 交付项 | 内容要求 |
|---|---|
| 接口族清单 | 按上述 5 类接口族列出模块对、触发条件、输入字段、输出字段、异常处理、刷新周期和协作人 |
| 字段字典 | 每个字段写清楚中文名、英文建议名、类型、单位、枚举值、是否必填、来源模块和消费模块 |
| 状态枚举 | 至少覆盖供电状态、车辆运行模式、牵引/制动状态、车门状态、故障等级、告警等级、检修闭锁状态 |
| 告警规则 | 写清楚告警来源、触发条件、等级建议、影响范围计算依据、是否推送调度、是否展示在综合监控 |
| 影响范围说明 | 供电故障需说明受影响供电分区、区段、列车列表；车辆故障需说明故障列车、后续列车和可能受影响客流 |
| 联调验收点 | 每个接口族至少给出正常状态、异常状态、数据中断、恢复后刷新四类验收场景 |
| 不实现边界 | 对维修工单、检修审批、经济成本、FMU 接入等暂不明确内容，写入边界清单，不在接口文档中默认承诺实现 |

### 4.3 建议字段颗粒度

| 类别 | 最低应覆盖字段 |
|---|---|
| 公共字段 | `timestamp`、`simulationTime`、`sourceModule`、`targetModule`、`traceId`、`dataQuality`、`heartbeatStatus` |
| 供电状态 | `powerSectionId`、`sectionName`、`startMileage`、`endMileage`、`voltage`、`current`、`load`、`energizedStatus`、`breakerStatus`、`faultCode` |
| 车辆状态 | `trainId`、`serviceNo`、`headMileage`、`tailMileage`、`speed`、`acceleration`、`operationMode`、`tractionState`、`brakeState`、`doorState`、`passengerLoad`、`loadRate` |
| 能耗与回馈 | `tractionPower`、`regenPower`、`netEnergy`、`cumulativeEnergy`、`powerDrawStatus`、`energyDataQuality` |
| 故障与告警 | `alarmId`、`alarmSource`、`alarmLevel`、`location`、`description`、`affectedSectionIds`、`affectedTrainIds`、`suggestedAction`、`ackStatus` |
| 检修与维修预留 | `maintenanceState`、`workOrderId`、`lockoutState`、`repairStatus`、`approvalState`、`operator`、`auditTime` |

### 4.4 不清晰边界清单

以下事项目前不应直接写成已确定实现，需要在接口文档中标注为“待确认”：

| 待确认问题 | 涉及人员 | 需要确认的边界 |
|---|---:|---|
| 供电能力下降由谁判定调度策略 | 方首岳、刘倩 | 方首岳是否只输出可用供电能力和受影响范围，限速/扣车/折返是否全部由刘倩判定 |
| 车辆载客量来源 | 方首岳、刘倩、廖宇轩 | 载客量是车辆模块模拟生成、客流模块输入，还是由综合监控汇总展示时计算 |
| 车厢级满载率是否实现 | 方首岳、廖宇轩 | 车辆侧是否提供单节车厢载客量，还是只提供整列满载率 |
| 车辆位置到供电分区映射来源 | 方首岳、黄旭涛 | 由轨道拓扑统一提供区段/公里标映射，还是供电模块单独维护分区里程范围 |
| 供电故障影响范围计算归属 | 方首岳、廖宇轩、刘倩 | 方首岳是否计算受影响列车列表，综合监控是否只展示，调度是否再计算策略影响 |
| 维修工单是否纳入本期范围 | 方首岳、顾远 | 车辆维修和供电检修是否只保留字段，还是要写完整工单流转接口 |
| 检修停送电审批是否实现 | 方首岳、刘倩、顾远 | 立项说明书中有检修流程，但本期是否只做状态展示和预留字段 |
| 能耗/经济分析深度 | 方首岳、廖宇轩、顾远 | 是否只输出牵引能耗和再生制动数据，票务收入、运营成本、能耗成本是否暂不实现 |
| FMU 接入边界 | 方首岳、顾远 | 当前接口文档是否只描述 `VehiclePhysicsClient` 抽象输入输出，不承诺 Python FMU 服务协议 |
| 供电遥控权限 | 方首岳、廖宇轩、顾远 | 断路器分合闸、母联切换、越区供电等遥控操作是否做真实控制接口，还是只做仿真状态和二次确认字段 |
| 告警等级统一标准 | 方首岳、廖宇轩、全员 | 供电和车辆故障等级如何映射到统一告警等级 1-3 |
| 数据刷新频率差异 | 方首岳、廖宇轩、顾远 | 车辆普通状态、车辆关键状态、供电状态、保护动作告警是否采用不同刷新频率 |

## 5. 按工作量口径核对

| 人员 | 大致工作量 | 主责范围 |
|---|---:|---|
| 方首岳 | 约 3 | 供电、车辆、供电—车辆、车辆/供电对监控和维修能耗的输出，字段多且状态复杂 |
| 刘倩 | 约 3 | 调度闭环、调度与车辆/信号/供电/客流/综合监控联动，负责策略输入输出和评估字段 |
| 黄旭涛 | 约 2 | 信号—轨道安全链路、信号/轨道对车辆和综合监控的数据输出，重点是安全约束和拓扑联动 |
| 廖宇轩 | 约 2 | 综合监控前端展示、统一告警、通信状态、权限配置、字段映射和联调验收，不主写业务逻辑 |

## 6. 最终提交建议

**方首岳提交：**
《供电与车辆接口族说明》
重点覆盖供电、车辆、能耗、维修检修预留，以及供电/车辆对综合监控的数据输出。

**刘倩提交：**
《运营调度接口族说明》
重点覆盖调度与车辆、信号/轨道、供电、客流、综合监控之间的运行策略闭环。

**黄旭涛提交：**
《信号与轨道接口族说明》
重点覆盖信号—轨道安全控制、车辆位置与 MA/限速联动、轨道拓扑查询和信号轨道告警。

**廖宇轩提交：**
《综合监控前端接入与公共支撑接口说明》
重点覆盖综合监控展示字段、统一告警、心跳通信、仿真同步展示、权限配置入口和联调验收记录。

最后统一汇总成：

**《上京地铁系统模块间交互协议说明》**
建议按“接口族、模块对、触发条件、输入字段、输出字段、异常处理、心跳/同步、权限与日志、刷新周期、主责人、协作人、联调状态”整理。这样比固定协议数量更适合当前立项说明书的范围。
