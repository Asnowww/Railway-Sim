# 城市轨道交通列车运行综合仿真系统
# 个 人 周 报
# 北京交通大学 · 学期实训III · "我为祖国开地铁"

## 一、基本信息

| 项目 | 内容 |
|------|------|
| 姓 名 | 黄旭涛 |
| 所属模块 | 信号轨道 |
| 周 次 | 第 1 周 |
| 日期范围 | 2026.07.05 — 07.10 |

## 二、本周工作内容

| 序号 | 任务描述 | 所属模块 | 完成状态 | 工时(h) | 优先级 |
|------|---------|---------|---------|---------|-------|
| 1 | 进路联锁十二态状态机实现（AVAILABLE→VALIDATING→LOCKED→OCCUPIED→RELEASING→RELEASED 及 CONFLICTED/REJECTED/FAILED/CANCELLED） | 信号 | 完成 | 8 | P0 |
| 2 | InterlockingFailureCode 13 种结构化失败码枚举，支持调度侧重试决策 | 信号 | 完成 | 3 | P1 |
| 3 | 固定闭塞 MA 计算（FixedBlockAuthorityCalculator）与站台停靠时间控制 | 信号 | 完成 | 6 | P0 |
| 4 | 调度-信号进路联调闭环：REQUEST_ROUTE/REROUTE/CANCEL_ROUTE 接入 advanceOneTick，按实际列车占用校验 | 信号 | 完成 | 5 | P0 |
| 5 | WP-02~05 接口实现：`/api/signal-track/routes`、`/route-events`、`/faults` 含审计字段 | 轨道 | 完成 | 4 | P1 |
| 6 | 配合车辆组：9300 DEPARTING_STATION 离站保护状态联调 | 信号 | 完成 | 2 | P1 |
| 7 | 车辆/供电控制设计文档更新，对齐 P0 变更 | 文档 | 完成 | 2 | P2 |

## 三、关键成果与产出

1. **进路联锁十二态状态机**：完成了 AVAILABLE→VALIDATING→LOCKED→OCCUPIED→RELEASING→RELEASED 主链及 CONFLICTED/REJECTED/FAILED/CANCELLED 副链的完整迁移路径，每步含前置条件校验（区段存在性、道岔可用性、占用检查、冲突检测），`RouteInterlockingServiceTests` 22 个测试覆盖全链路
2. **固定闭塞 MA 计算**：补充站停速度控制逻辑，列车到站窗口后 MA 截至站台位置，停站时间到后自动释放
3. **调度-信号联调闭环**：进路命令（REQUEST_ROUTE 等）统一由 `applyDispatchCommand()` 消费并返回结构化结果，区分成功/失败/重试
4. **WP02-05 接口收口**：`/api/signal-track` 下全部写接口增加 `operator + traceId` 审计和区段存在性前置校验
5. **代码提交**：本周期约 15 次提交，覆盖 `RouteInterlockingService`、`SignalService`、`SimulationRuntime`、`SignalTrackController` 等核心文件

## 四、遇到的问题与解决方案

| 序号 | 问题描述 | 解决方案 / 当前进展 | 状态 |
|------|---------|------------------|------|
| 1 | 进路状态迁移路径不明确，部分跳转缺少前置条件 | 定义了 `RouteState.transitionTo()` 的合法迁移表，非法迁移直接抛 `IllegalStateException`，后续统一走 `reject()` → `REJECTED/FAILED` | 已解决 |
| 2 | 固定闭塞模式站停后速度不归零 | 在 `FixedBlockAuthorityCalculator` 后处理中增加 `constrainedSpeed = 0` 的条件分支，站台停靠覆盖进路限制优先级 | 已解决 |
| 3 | 调度命令进入执行链路后缺少信号侧反馈通道 | 调度侧绕用 `/api/dispatch/route/establish` 调试接口，不走正式命令记录。与调度沟通后在 `advanceOneTick` 中加入进路命令消费流程 + feedback 回推 | 已解决 |

## 五、下周工作计划

| 序号 | 计划任务 | 所属模块 | 预计工时(h) |
|------|---------|---------|-----------|
| 1 | M9 北京地铁真实线路数据接入，长段按站间距切分为 47 段 | 信号轨道 | 12 |
| 2 | 上下行双线独立运行：kmForwardMap + 四层轨道过滤 | 信号轨道 | 16 |
| 3 | 调度全自动发车闭环：移除硬编码初始车 + 删除 DOWN 门禁 | 信号轨道 | 8 |
| 4 | 三层命令反馈闭环：接收回执 → 应用反馈 → 效果确认 | 信号轨道 | 10 |
| 5 | 占用/预留/联锁联动修复（overlaps/hold解除/终点释放） | 信号轨道 | 10 |

## 六、备注

- 进路状态机对调度进路命令的反馈目前是字符串格式，下周需与调度沟通统一的 `InterlockingFeedback` 结构
- 9300 离站保护状态（DEPARTING_STATION）已合并入 dev，但需要在 M9 线路上做联调验证
